# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is currently empty — there is no source code, README, or dependency manifest yet. The only files present are placeholder `.claude/` configuration files (`CLAUDE.md`, `.mcp.json`, `settings.json`, `settings.local.json`), all of which are empty, plus empty `agents/`, `rules/`, and `skills/` directories.

There are no build, lint, or test commands to document yet, and no architecture to describe.

When real code is added to this project, update this file with:
1. Commands to build, lint, test, and run the project (including how to run a single test).
2. The high-level architecture and structure of the codebase.
